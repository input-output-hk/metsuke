# Changelog

Three things are released from this repository and they are versioned apart, so
each has its own section here. `docs/releasing.md` is the procedure, ADR 0006 is
why. `metsuke-wire` is not released and does not appear.

## metsuke, the agent

### Unreleased

Nothing tagged yet. The first entry here is written when a `client-v*` tag
exists.

## metsuke-server

### Unreleased

Nothing tagged yet. Same, for `server-v*`.

## metsuke-fetch

### Unreleased

Nothing tagged yet. Same, for `fetch-v*`.
